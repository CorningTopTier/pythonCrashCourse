numbers = list(range(1, 1000001)) # create the list

minimum = min(numbers) # check min value
maximum = max(numbers) # check max value
print(f"Minimum: {minimum}, Maximum: {maximum}")

total = sum(numbers) # sum of the numbers
print(f"Sum: {total}")